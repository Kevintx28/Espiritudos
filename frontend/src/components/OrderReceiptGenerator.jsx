import React, { useEffect, useRef, useState } from 'react';
import html2canvas from 'html2canvas';
export default function OrderReceiptGenerator({ order, onGenerated }) { const ref = useRef(null); const [error, setError] = useState(''); useEffect(() => { html2canvas(ref.current, { scale: 2, backgroundColor: '#0b0812', logging: false }).then((canvas) => onGenerated(canvas.toDataURL('image/png'))).catch(() => setError('No se pudo generar la imagen local. Intenta de nuevo.')); }, [onGenerated]); return <>{error && <p role="alert" className="text-red-200">{error}</p>}<div ref={ref} className="fixed -left-[9999px] top-0 w-[900px] bg-[#0b0812] p-12 font-sans text-white"><h1 className="text-5xl font-black text-purple-300">KTXStore</h1><p className="mt-2 text-xl text-cyan-300">Pedido {order.id}</p><p className="text-slate-400">{order.date} · {order.country.name}</p><div className="mt-8 rounded-2xl bg-[#171222] p-6"><h2 className="text-2xl font-bold">{order.game.name}</h2>{order.items.map(({ product, quantity }) => <p key={product.id} className="mt-3">{product.name} × {quantity} · {product.price === null ? 'PRUEBA' : `S/ ${(product.price * quantity).toFixed(2)}`}</p>)}<p className="mt-6 text-3xl font-black text-cyan-300">Total: S/ {order.total.toFixed(2)}</p></div><div className="mt-6 rounded-2xl bg-[#171222] p-6"><p>Nombre: {order.form.name}</p><p>Contacto: {order.form.contact}</p>{order.form.identifier && <p>Identificador: {order.form.identifier.replace(/.(?=.{3})/g, '*')}</p>}<p>Método: {order.paymentMethod}</p><p className="mt-4 font-bold text-amber-300">Estado: Pendiente de verificación</p>{order.testOnly && <p className="mt-3 font-black text-red-300">PRUEBA — NO PAGAR</p>}</div><p className="mt-8 text-lg text-slate-300">Envía esta imagen y tu comprobante a KTXStore para procesar tu pedido.</p></div></>; }
