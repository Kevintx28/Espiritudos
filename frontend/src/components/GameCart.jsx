import React from 'react';
import { Minus, Plus, Trash2 } from 'lucide-react';

export default function GameCart({ items, onChange, onClear, onContinue }) {
  const total = items.reduce((sum, item) => sum + (item.product.price || 0) * item.quantity, 0);
  return <aside className="rounded-2xl border border-cyan-400/20 bg-[#11101a] p-5 lg:sticky lg:top-4"><div className="flex items-center justify-between"><h2 className="text-xl font-bold text-white">Carrito del juego</h2><button type="button" onClick={onClear} className="text-xs text-red-300 hover:text-red-200"><Trash2 className="mr-1 inline h-4 w-4" />Vaciar</button></div>{items.length === 0 ? <p className="py-8 text-sm text-slate-500">Añade un producto para continuar.</p> : <div className="mt-5 space-y-4">{items.map(({ product, quantity }) => <div key={product.id} className="border-b border-white/10 pb-4"><div className="flex justify-between gap-3"><span className="text-sm text-white">{product.name}</span><span className="text-sm font-bold text-cyan-300">{product.price === null ? 'Prueba' : `S/ ${(product.price * quantity).toFixed(2)}`}</span></div><div className="mt-2 flex items-center gap-2"><button type="button" aria-label={`Reducir ${product.name}`} onClick={() => onChange(product, quantity - 1)} className="rounded border border-white/20 p-1"><Minus className="h-3 w-3" /></button><span className="w-6 text-center text-sm">{quantity}</span><button type="button" aria-label={`Aumentar ${product.name}`} onClick={() => onChange(product, quantity + 1)} className="rounded border border-white/20 p-1"><Plus className="h-3 w-3" /></button></div></div>)}<div className="flex justify-between text-lg font-black text-white"><span>Total</span><span>S/ {total.toFixed(2)}</span></div><p className="text-xs text-slate-500">Moneda: PEN (S/)</p><button type="button" data-testid="cart-continue" onClick={onContinue} className="w-full rounded-xl bg-cyan-400 px-4 py-3 font-black text-slate-950">Continuar</button></div>}</aside>;
}
