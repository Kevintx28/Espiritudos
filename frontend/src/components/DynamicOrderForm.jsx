import React, { useState } from 'react';
import { ArrowLeft, ArrowRight } from 'lucide-react';
export default function DynamicOrderForm({ game, initialValue, onBack, onContinue }) { const [form, setForm] = useState(initialValue || { name: '', contact: '', identifier: '', note: '' }); const [error, setError] = useState(''); const identifierLabel = { player_id: 'Player ID', account_id: 'ID de cuenta', numeric_uid: 'UID numérico' }[game.deliveryType]; const needsIdentifier = Boolean(identifierLabel); const submit = (event) => { event.preventDefault(); if (!form.name.trim() || !form.contact.trim()) return setError('Completa tu nombre y contacto.'); if (needsIdentifier && !form.identifier.trim()) return setError(`Completa el campo ${identifierLabel}.`); if (game.deliveryType === 'numeric_uid' && !/^\d+$/.test(form.identifier)) return setError('El UID debe contener solo números.'); if (game.deliveryType === 'player_id' && !/^\d+$/.test(form.identifier)) return setError('El Player ID debe contener solo números.'); setError(''); onContinue(form); }; return <form onSubmit={submit} className="mx-auto max-w-2xl rounded-2xl border border-white/10 bg-[#15131f] p-6"><p className="text-sm uppercase tracking-[0.2em] text-cyan-300">Datos</p><h2 className="mt-2 text-2xl font-bold text-white">Datos para validar el pedido</h2><p className="mt-2 text-sm text-slate-400">Nunca pedimos contraseñas, sesiones, 2FA ni credenciales del juego.</p><div className="mt-6 grid gap-4"><label className="text-sm text-slate-300">Nombre visible<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 p-3 text-white" /></label><label className="text-sm text-slate-300">WhatsApp o Discord<input required value={form.contact} onChange={(e) => setForm({ ...form, contact: e.target.value })} className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 p-3 text-white" /></label>{needsIdentifier && <label className="text-sm text-slate-300">{identifierLabel}<input required inputMode={game.deliveryType === 'numeric_uid' || game.deliveryType === 'player_id' ? 'numeric' : 'text'} value={form.identifier} onChange={(e) => setForm({ ...form, identifier: e.target.value })} className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 p-3 text-white" /></label>}<label className="text-sm text-slate-300">Nota opcional<textarea value={form.note} onChange={(e) => setForm({ ...form, note: e.target.value })} className="mt-2 min-h-24 w-full rounded-xl border border-white/10 bg-black/30 p-3 text-white" /></label></div>{error && <p role="alert" className="mt-4 rounded-xl bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}<div className="mt-6 flex justify-between gap-3"><button type="button" onClick={onBack} className="flex items-center gap-2 rounded-xl border border-white/15 px-4 py-3 text-slate-200"><ArrowLeft className="h-4 w-4" />Atrás</button><button type="submit" className="flex items-center gap-2 rounded-xl bg-cyan-400 px-4 py-3 font-bold text-slate-950">Pago <ArrowRight className="h-4 w-4" /></button></div></form>; }
